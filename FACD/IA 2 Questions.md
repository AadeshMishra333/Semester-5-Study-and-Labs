# Finite Automata and Compiler Design — Questions & Selected Answers

## Question 1

**Which of the following statements is/are TRUE?**

* [x] **Sentinel avoids explicit end-of-buffer checks**
* [ ] A single buffer is always sufficient
* [x] **Input buffering reduces disk access**
* [x] **Buffer pair improves performance**

---

## Question 2

**Consider the following regular definitions:**

$$M_1 \rightarrow (a \mid \epsilon)(b \mid c)^*a$$

$$M_2 \rightarrow (b \mid \epsilon)(a \mid c)^*b$$

$$M_3 \rightarrow (c \mid \epsilon)(b \mid a)^*c$$

A lexical analyser processes the input string: **bbaacbc**. The analyser selects the token whose pattern matches the longest possible prefix of the remaining input. Which of the following statements are correct?

* [x] **During the first scan, the longest prefixes matching $M_1, M_2,$ and $M_3$ are bba, bb, and bbaac, respectively.**
* [ ] The first lexeme recognized is bba, because the accepting state for $M_1$ is reached before the accepting state for $M_3$.
* [ ] After recognizing the first token, the remaining input is abc.
* [x] **The final token sequence produced by the lexical analyser is $M_3M_3$.**
* [ ] For the remaining input abc, the longest prefixes matching $M_1, M_2,$ and $M_3$ are a, ab, and abc, respectively.

---

## Question 3

**Consider these C code segments:**

**1.**

```c
int a@b;

```

**2.**

```c
a = b++c;

```

**3.**

```c
int x;
char *y;
x = y + 10;

```

**Which statements are correct?**

* [x] **Segment 1 is most likely detected as erroneous during lexical analysis.**
* [ ] Segment 2 contains a lexical error because ++ is not a valid token.
* [ ] Segment 2 is tokenized successfully but rejected during syntax analysis.
* [x] **Segment 3 is checked during semantic analysis because it involves incompatible types.**
* [ ] Segment 3 contains a syntax error because pointer arithmetic is never grammatically valid.

---

## Question 4

**Consider the following context-free grammar $G$, where $S,A,B$ are the variables (non-terminals), and $a,b$ are the terminal symbols, with $S$ as the start variable.**

Productions:

$$S \rightarrow aaB \mid Abb$$

$$A \rightarrow a \mid aA$$

$$B \rightarrow b \mid bB$$

**Which of the following represents the language $L(G)$ generated by $G$?**

* [x] **$L(G)=\{a^2b^n\mid n\ge1\}\cup\{a^nb^2\mid n\ge1\}$**
* [ ] $L(G)=\{a^nb^n\mid n\ge1\}$
* [x] **Every string generated using the production $S\rightarrow aaB$ contains exactly two a's, while every string generated using $S\rightarrow Abb$ contains exactly two b's.**
* [ ] $L(G)=\{a^nb^{2n}\mid n\ge1\}\cup\{a^{2n}b^n\mid n\ge1\}$

---

## Question 5

**A PDA can accept a string by:**

* [ ] Empty stack only
* [x] **Either final state or empty stack**
* [ ] Final state only
* [ ] Neither final state nor empty stack

---

## Question 6

**Which of the following errors are generally not detected during the compiler's analysis phases?**

* [ ] Use of an undeclared variable
* [x] **Division by zero when the divisor is determined only at runtime**
* [ ] Type mismatch
* [x] **Infinite loop**

---

## Question 7

**Consider the following C statement:**

```c
float total = rate * 10.5;

```

The lexical analyzer recognizes the tokens and creates or accesses a symbol-table entry for `rate`. Later, `rate` is found to be undeclared.

**Which statements are correct?**

* [x] **Semantic analysis reports an error because rate has not been declared in the accessible scope.**
* [x] **The lexical analyzer recognizes rate as an identifier based on its character pattern.**
* [x] **The syntax analyzer can successfully parse the statement if it conforms to the grammar.**
* [ ] The lexical analyzer must reject rate because it has not been declared.
* [ ] The syntax analyzer reports the undeclared-variable error because declaration checking is purely syntactic.

---

## Question 8

**Suppose $L_1$ is regular and $L_2$ is context-free over alphabet $\Sigma$. Which of the following languages are not necessarily context-free?**

* [x] **$L_1 - L_2$**
* [ ] $L_1 \cup L_2$
* [x] **$\Sigma^* - L_2$**
* [ ] $L_1 \cap L_2$

---

## Question 9

**Which of the following languages typically requires a PDA rather than a finite automaton?**

* [ ] $L = \{a, b\}^*$
* [x] **$L = {w \in {a, b}^* \mid w = w^R}$**
* [x] **$L = \{a^n b^n \mid n \ge 0\}$**
* [ ] $L = \{a^n \mid n \ge 0\}$

---

## Question 10

**Consider the following statement:**

```text
ifValue = if + 1

```

Assume that `if` is a reserved keyword and identifiers follow the pattern:

```text
letter (letter | digit)*

```

**Which of the following statements are correct?**

* [x] **The standalone lexeme `if` is recognized as the keyword token IF.**
* [ ] A lexical error occurs because `ifValue` begins with a reserved keyword.
* [x] **`ifValue` is recognized as a single identifier.**
* [ ] `ifValue` is divided into the tokens IF and ID (Value).
* [x] **The complete token sequence is: <ID,1>    <1>**
